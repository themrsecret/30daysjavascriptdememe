/*
let fullName

    try {
        let firstName = 'tayfun'
        let lastName = 'erbilen'
        fullName = firstName + ' ' + lastName
    
        throw new Error('bir hata var!')
    } catch (e) {
        console.log(e.name)
        console.table(e)
    }

    console.log(fullName)
*/

let age = pro('kaç yasindasin?')

/*
try {
    if (age > 29) throw new Error('Yalan soyluyorsun!')
} catch (e) {
    alert(e.message)
}
*/

let firstName = 'tayfun'
let lastName = 'erbilen'
//let fullName = firstName + ' ' + surname

//console.log(5 x 5)

console.log(
    firstName.toFixed(2)
)